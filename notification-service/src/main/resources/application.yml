server:
  port: 8081

spring:
  application:
    name: notification-service
  datasource:
    url: jdbc:postgresql://${POSTGRES_NOTIFICATIONS_HOST:localhost}:${POSTGRES_NOTIFICATIONS_PORT:5433}/${POSTGRES_NOTIFICATIONS_DB:notifications_db}
    username: ${POSTGRES_NOTIFICATIONS_USER:notifications_user}
    password: ${POSTGRES_NOTIFICATIONS_PASSWORD:notifications_pass}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USERNAME:guest}
    password: ${RABBITMQ_PASSWORD:guest}
  jackson:
    serialization:
      write-dates-as-timestamps: false

application:
  rabbitmq:
    exchange: notifications.exchange
    queue: notifications.queue
    routing-key: notifications.#

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

logging:
  level:
    com.autumnus.notificationservice: DEBUG
    org.springframework.amqp: INFO
    org.springframework.messaging: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
